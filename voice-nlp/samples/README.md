# Amostras de audio para testes

Arquivos de exemplo para validar STT e `POST /api/voice`. Podem ser versionados (tamanho pequeno).

| Arquivo | Uso |
|---------|-----|
| `audio_piloto.ogg` | Gravacao de teste do piloto (pergunta em voz) |